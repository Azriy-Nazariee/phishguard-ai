# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Specify the frontend directory where your package.json is located
      - name: Install dependencies and build frontend
        working-directory: ./frontend  # Add this line to specify the directory
        run: |
          npm ci
          npm run build
          
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PHISHGUARD_AI_F8E71 }}
          channelId: live
          projectId: phishguard-ai-f8e71
